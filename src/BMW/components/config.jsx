import React from "react";
import PropTypes from "prop-types";

const Config = ({}) => (
	<div id="Config">
		<p>This is config</p>
	</div>
);

Config.defaultProps = {
	//Something
};

Config.propTypes = {
	//Something
};

export default Config;
