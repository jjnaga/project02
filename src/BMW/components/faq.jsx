import React from "react";
import PropTypes from "prop-types";

const FAQ = ({}) => (
	<div id="FAQ">
		<p>This is FAQ</p>
	</div>
);

FAQ.defaultProps = {
	//Something
};

FAQ.propTypes = {
	//Something
};

export default FAQ;
